'use strict';

console.log ('Figury z użyciem flex-a');

var docHeight = document.documentElement.offsetHeight;//wysokość całego dokumentu
console.log(docHeight);

var docWidth = document.documentElement.offsetWidth;//szerokość całego dokumentu
console.log(docWidth);
